% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replaceQAreas.R
\name{replaceQAreas}
\alias{replaceQAreas}
\title{Modify the area weights from a quadscheme to correct for observation bias}
\usage{
replaceQAreas(
  Q = NULL,
  bias.data = NULL,
  im = NULL,
  positive = TRUE,
  kernel = "gaussian",
  sigma = NULL,
  varcov = NULL,
  weights = NULL,
  edge = TRUE
)
}
\arguments{
\item{Q}{A quad object}

\item{bias.data}{A two o three-column data frame containing the points of the sampling localitites, 
where the first two should be the x and y coordinates and the third the weights. Alternatively, a SpatRaster or
im object representing the spatial variability of observation effor.}

\item{im}{an im object with the same resolution of the quadrature scheme in the Q argument. 
observation localitites if bias.data is a set of point localities.}

\item{positive}{Logical, specciffying whether density of sampling localities should be positive.}

\item{kernel}{A character string, specifying the type of kernel for the sampling localities.
The defaiult value is "gaussian", but all the types of kernels in spatstat.explore::density.ppm are supported.}

\item{sigma}{A positive real-valued numeric, indicating the standard deviation in distance units to generate the kernel density}

\item{varcov}{A two by two matrix for the covariance between x and y sampling localities if these are correlated}

\item{weights}{A numeric vector with the length of sampling localities with the weights for each observation.}

\item{edge}{Logical, whether to perform edge correction}
}
\value{
A quadscheme but with modified, spatially variable area weights.
}
\description{
If observation bias is believed to affect a point process, and sampling effort variability is understood,
this function provides a way of correcting for sampling bias, by modiffying the size of the area weights
in relation to an assumed uniform sampling scenario.
}
\examples{
r <- system.file("extdata", "ChelsaBio.tif", package = "espatsmo") |> terra::rast() |> scale()

p <- system.file("extdata", "points.csv", package = "espatsmo") |> read.csv()

bias <- system.file("extdata", "Target-group.tif", package = "espatsmo") |>  terra::rast()

iml <- imFromStack(r)

p.pp <- spatstat.geom::ppp(x = p$x, y = p$y, window = as.owin(iml[[1]]))

Q <- spatstat.geom::pixelquad(p.pp)

QA <- replaceQareas(Q = Q,
                    bias.data = bias,
                    im = iml)
}
