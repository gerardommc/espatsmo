% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getPolyFormulas.R
\name{getPolyFormulas}
\alias{getPolyFormulas}
\title{Automatically generate polynomial formulas using a covariate compatibility matrix and the exponents for each variable}
\usage{
getPolyFormulas(respDF = NULL, compatMat = NULL)
}
\arguments{
\item{respDF}{A data.frame containing two columns with names "Variable" and "Power", where foreach variable name in the compatibility matrix returned by findCompatibles, there should be a proposed positive integer exponent.}

\item{compatMat}{A covariate compatibility matrix generated with findCompatibles.}
}
\value{
A character vector containing the model formulas for each row of variable cobinations in compatMat, and here each term in the formula has an exponent 1:Power.
}
\description{
This function takes two arguments, a data.fame with two columns with the specific names "Variable" and "Power",
and a covariate compatibility matrix, usually generated with the function findCompatibles. The variable names provided in the respDF argument
MUST match those in the covariate compatibility matrix. Both arguments are used to generate a "character" type of vector, where each element
is a string convertible to a formula object. the resulting formulas contain a polynomial of the order specified in respDF funciton of 
each subset of variables in compatMat
}
\examples{
r <- system.file("extdata", "ChelsaBio.tif", package = "espatsmo") |>  terra::rast()

resp <- system.file("extdata", "Exponents.csv", package = "espatsmo") |>  read.csv()

compat <- findCompatibles(covariates = r,
                          thres = 0.6,
                          max.comb = 3)

forms <- getPolyFormulas(respDF = resp, 
                         compatMat = compat)
}
