% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppmLGCP.R
\name{ppmLGCP}
\alias{ppmLGCP}
\title{Fit a log-Gaussian Cox point process model}
\usage{
ppmLGCP(
  points = NULL,
  covariates = NULL,
  formula = NULL,
  offset = NULL,
  no.bkgd = NULL,
  group.bins = 50,
  bias.data = NULL,
  bias.correction = NULL,
  covariate.zero = NULL,
  covariance.func = "pcmatern",
  weight.bias.conf = list(positive = TRUE, kernel = "gaussian", sigma = NULL, varcov =
    NULL, weights = NULL, edge = TRUE),
  prior.conf = list(alpha = 2, prior.range = c(10000, 0.01), prior.sigma = c(0.05, 0.01)),
  mesh.par = list(edge = 5000, offset = 10000),
  coordinates = "m",
  dist.units = "m",
  weight.units = "km",
  dist.ar = T,
  verbose = T,
  inla.mode = "experimental"
)
}
\arguments{
\item{points}{A two-clumn data.frame, ppp or quad object containing the presence localities}

\item{covariates}{A SpatRaster or imList containing the environmental covariates}

\item{formula}{character, speciftying the right hand side of a linear combination of covariate predictors}

\item{offset}{character, specifying the name of the covariate to use as the population at risk or the population density from which presence points were drawn.}

\item{no.bkgd}{numeric integer, specifying the number of background or quadratre points used to fit the model}

\item{group.bins}{numeric integer, specifying the numbr off bins into which the distance to presence points will be divided in order to fit the autoregressive random effect for distance to presence points}

\item{bias.data}{A two-column data frame, containing the coordinates of the sampling localities, 
SpatRaster or imList object containing the spatial variability of the observation effort}

\item{bias.correction}{character, with values 'weights' or 'background', 
specifying the method used to control the effect of sampling bias}

\item{covariate.zero}{Character, indicating the variable name representing observer bias within the model formula. Must be a variable contained in the covariates raster stack}

\item{covariance.func}{character, specifying the name of the covariance function for the gaussian process, either "pcmatern", or "matern". Please check how these options should be configured 
via the `prior.conf` argument.}

\item{weight.bias.conf}{list, containing the following elements: 1) positive, 2) kernel, 3) sigma, 4) varcov, 5) weights, 6) edge}

\item{prior.conf}{list, with two different specifications depending on the covariance function selected. 
If `covariance.func = "pcmatern"`, the list must provide values for: 1) alpha = 2,
2) prior.range = c(100, 0.01), 3) prior.sigma = c(0.05, 0.01)).
If `covariance.func = "matern"`, the list must provide values for:
1) sigma0 = 0.05, 2) range0 = 0.2, 3) alpha = 2, 4) kappa0 = sqrt(8)/20,
5) B.tau = c(min = -1, max = 1), 6) B.kappa = c(max = 0, min = -1), 7)theta.prior.mean = c(0, 0),
8)theta.prior.prec = c(0.1, 1).
For in-depth descrition of the meaning of these parameters, the reades should consult INLA guidelines
for both covariance functions and particularly adjust the distance-dependent arguments for the 
coordinate units of the working point process.}

\item{mesh.par}{list, which must provide values for: 1) edge = 50, 
and 2) offset = 50; used to configure the projector mesh to integrate the random field.}

\item{coordinates}{character, used to specify the x and y units in which the covariance 
matrix will be computed, and takes two possible values, "m" or "km".}

\item{dist.units}{character, used to specify the distance units in which the covariance 
matrix will be computed, and takes two possible values, "m" or "km".}

\item{weight.units}{= "km",}

\item{verbose}{logical, to configure whether the model fitting process will bee printed on screen in real time.}

\item{inla.mode}{character, to specify whether the "experimental" of NULL model will be used}

\item{ditst.ar}{Logical, indicating whether to include an autorregressive random effect for distance to presence points}
}
\value{
list of class `ppmLGCP` containing the fitted model, call, mesh and predictions for fixed and random effects.
}
\description{
Fit a log-Gaussian Cox point process model using INLA, with the posibility of controlling observer bias
by modifying area weights or the distribution of background (quadrature) points.
}
\examples{
\dontrun{
r <- terra::rast("inst/extdata/ChelsaBio.tif")

p <- read.csv("inst/extdata/points.csv")

bias <- terra::rast("inst/extdata/Target-group.tif")

model <- ppmLGCP(points= p, 
                 covariates = r, 
                 formula = "~ bio1 + bio2 + bio12 + I(bio1^2) + I(bio2^2) + I(bio12^2)", 
                 bias.data = bias,
                 bias.correction = "weights")
}
}
